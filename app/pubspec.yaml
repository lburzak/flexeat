name: flexeat
description: Flexible recipes and food budget planning

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.7.0
  flutter_bloc: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  bloc_test: ^9.0.3
  mockito: ^5.1.0
  build_runner: ^2.1.10

flutter:
  uses-material-design: true
